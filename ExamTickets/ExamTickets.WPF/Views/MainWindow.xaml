<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="ExamTickets.WPF.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="ExamTickets"
        Width="1400"
        Height="900"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MaterialDesignPaper}"
        Foreground="{DynamicResource MaterialDesignBody}">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Window.Resources>

    <Grid Background="{DynamicResource MaterialDesignPaper}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Background="{DynamicResource MaterialDesignPaper}"
                BorderBrush="{DynamicResource MaterialDesignDivider}"
                BorderThickness="0,0,1,0">
            <StackPanel Margin="16">
                <TextBlock Text="{Binding DataContext.UserFullName, RelativeSource={RelativeSource AncestorType=Window}}"
                           FontWeight="Bold"
                           Margin="0,0,0,4"/>

                <Button Content="Создание билетов"
                        Command="{Binding NavigateToTicketCreationCommand}"
                        Margin="0,0,0,8"/>
                <Button Content="Дашборд"
                        Command="{Binding NavigateToDashboardCommand}"
                        Margin="0,0,0,8"
                        Visibility="{Binding IsNotTeacher, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                <Button Content="Справочники"
                        Command="{Binding NavigateToDatabaseCommand}"
                        Margin="0,0,0,8"
                        Visibility="{Binding IsNotTeacher, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                <Button Content="Пользователи"
                        Command="{Binding NavigateToUsersCommand}"
                        Margin="0,0,0,8"
                        Visibility="{Binding IsAdmin, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                <Button Content="История"
                        Command="{Binding NavigateToHistoryCommand}"
                        Margin="0,0,0,8"/>
                <Button Content="Журнал аудита"
                        Command="{Binding NavigateToAuditLogCommand}"
                        Margin="0,0,0,8"
                        Visibility="{Binding IsAdmin, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                <Button Content="Настройки"
                        Command="{Binding NavigateToSettingsCommand}"
                        Margin="0,0,0,8"/>
                <Button Content="Выйти"
                        Command="{Binding LogoutCommand}"
                        Margin="0,40,0,0"
                        Background="#CC3333"
                        Foreground="White"/>
            </StackPanel>
        </Border>

        <Frame Grid.Column="1"
               NavigationUIVisibility="Hidden"
               Content="{Binding CurrentPage}" />
    </Grid>
</Window>