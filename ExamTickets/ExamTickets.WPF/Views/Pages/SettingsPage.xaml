<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="ExamTickets.WPF.Views.Pages.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d"
      Title="Настройки">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Page.Resources>

    <Grid Margin="16" Background="{DynamicResource MaterialDesignPaper}">
        <StackPanel Width="480" HorizontalAlignment="Left">
            <TextBlock Text="Название учреждения" Margin="0,0,0,6"/>
            <TextBox Text="{Binding InstitutionName, UpdateSourceTrigger=PropertyChanged}"
                     IsReadOnly="{Binding IsReadOnly}"
                     Margin="0,0,0,6" />

            <TextBlock Text="Изменение доступно только администратору и председателю"
                       Foreground="Gray"
                       FontSize="11"
                       Visibility="{Binding IsReadOnly, Converter={StaticResource BooleanToVisibilityConverter}}"
                       Margin="0,0,0,12"/>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                <TextBlock Text="Тема" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox Width="120"
                          ItemsSource="{Binding Themes}"
                          SelectedItem="{Binding SelectedTheme}" />
            </StackPanel>

            <Button Content="Сохранить" Command="{Binding SaveSettingsCommand}" Width="140" HorizontalAlignment="Left"/>
        </StackPanel>
    </Grid>
</Page>